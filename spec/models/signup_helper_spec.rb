require 'spec_helper'
require 'factory_girl'
 

describe SignupHelper do
 
  
  # it "should not be considered valid if params hash does not contain at least one check item" do
  #   @helper = SignupHelper.new(Hash.new,Object.new)
  #   @helper.should_not be_valid
  # end

  # it "should not be considered valid if params hash does not contain at least one signup or switch request" do
  #   params = Hash.new
  #   params[:signup_ids] = []
  #   @helper = SignupHelper.new(params,Object.new)
  #   @helper.should be_valid
  # end
  
  # it "should not be considered valid if params hash does not contain at least one signup or switch request" do
  #   params = Hash.new
  #   params[:switch_ids] = []
  #   @helper = SignupHelper.new(params,Object.new)
  #   @helper.should be_valid
  # end

end