<%= form_for([@club,@role_group]) do |f| %>
  <% if @role_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_group.errors.count, "error") %> prohibited this role_group from being saved:</h2>

      <ul>
      <% @role_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <table>
    <tr>
      <td>
        <label>Description:</label>
        <%= f.text_field :description, :class => 'input-text' %><br>
      </td>
    </tr>
  </table>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
