



<%= form_for([current_club,@agenda_definition,@agenda_bottom]) do |f| %>
  <% if @agenda_bottom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agenda_bottom.errors.count, "error") %> prohibited this agenda_bottom from being saved:</h2>

      <ul>
      <% @agenda_bottom.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :agenda_definition_id %>
  </div>
  <div class="field">
    <%= f.label :heading_text %><br />
    <%= f.text_field :heading_text %>
  </div>

  <div class="field">
    <%= f.label :body_text %><br />
    <%= f.text_area :body_text, :rows => 5 %>
  </div>
  <div style='font-size:7pt'>
    <div   >
      <%= f.submit  "Create/Save Agenda Bottom" %>
    </div>
  </div>
<% end %>
